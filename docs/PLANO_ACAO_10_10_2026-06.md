# Plano de Ação 10/10 — Sistema Pet (auditoria profunda 2026-06)

Atualizado em: 2026-06-08 (mantido vivo ao longo da execução)

## Para que serve este documento

Este é o **passo a passo para levar o Sistema Pet a 10/10 de verdade** — não "bom para uso
interno", mas **robusto para produção com dados reais de várias lojas**.

Ele nasce da auditoria profunda de 2026-06-08 (backend, frontend, mobile, deploy, testes,
segurança), que encontrou pontos que o guia anterior (`docs/MATURIDADE_GERAL_10_10_GUIA.md`)
considerava "fechados para uso interno". Os dois convivem:

- `MATURIDADE_GERAL_10_10_GUIA.md` = maturidade para **uso interno profissional**.
- **Este arquivo** = caminho para **10/10 real de produção** (o nível mais exigente).

Regra de uso: marcar `[x]` só com PR mergeado + validação. Atualizar a cada passo.

---

## Placar honesto (escala 0–10)

| Área | Hoje (08/06) | Meta | O que falta em 1 frase |
|---|---:|---:|---|
| 🔒 Segurança | 8 | 10 | Isolamento automático fechado; falta RLS (defesa final) + endurecimentos de CI/segurança |
| 🚀 DevOps / Deploy | 5 | 10 | Deploy automático, staging, alertas (Sentry), limites de recursos |
| 📦 Backend – bibliotecas | 5 | 10 | Atualizar o "núcleo" (~2 anos atrás) + lockfile |
| 🏗️ Backend – arquitetura | 4 | 10 | Quebrar arquivos-gigantes, 1 sistema de eventos, modelos organizados |
| 🖥️ Frontend – arquitetura | 4 | 10 | Quebrar telas-gigantes, 1 cliente de API, type-safety |
| 📦 Frontend – bibliotecas | 6 | 10 | React 19, Vite 8, Tailwind 4, etc. (um por vez) |
| 📱 Mobile | 6 | 10 | Atualizar Expo, rastreio em background, alerta de erros |
| 🧪 Testes / Qualidade | 4 | 10 | Consertar ~149 testes, CI rodar tudo + travar, testes de frontend |

**Média hoje (11/06) ≈ 5,7/10. Meta: 10/10.**

> O maior abate de risco já feito hoje: o risco nº 1 (dados de uma loja vazarem para outra)
> saiu de "totalmente aberto" para "isolamento automático fechado pela catraca"; falta RLS como defesa final no banco.

---

## Como vamos trabalhar (princípios — valem para TODA mudança)

1. **Mudanças pequenas, um PR de cada vez.** Nada de "tudo de uma vez".
2. **Toda mudança de banco passa por homologação (staging) antes da produção.** (Hoje não existe staging — é o item 0.2, e destrava quase tudo.)
3. **Backup automático antes de migração** já está no ar (✅) — toda subida tem rede.
4. **Produção só com sua autorização explícita.** Nunca no susto.
5. **Rollback sempre pensado antes** (como desfazer se der errado).
6. **Validar com testes** — por isso consertar os testes (0.3) vem cedo.

---

## As 6 fases (em ordem de dependência)

> A ordem não é por "importância" pura, e sim por **o que destrava o quê**. Primeiro a gente
> ganha a capacidade de mexer com segurança (Fase 0), depois fecha riscos (Fase 1), moderniza
> (Fase 2), organiza (Fase 3), profissionaliza a operação (Fase 4) e poli o produto (Fase 5).

Legenda de esforço: **P** = pequeno (horas) · **M** = médio (dias) · **G** = grande (semanas).

---

### FASE 0 — Poder trabalhar com segurança (a fundação)
*Destrava todo o resto. Sem isso, qualquer mudança grande é arriscada.*

- [x] **0.1 Backup automático do banco antes de cada migração** — `P` — ✅ FEITO (PR #309, validado em produção: dump de 21 MB).
- [x] **0.2 Homologação antes da produção** — `M` — ✅ **DECIDIDO 2026-06-09 (Lucas): sem servidor de staging separado por enquanto (caminho barato).** As mudanças que **mexem no banco** são testadas restaurando uma **cópia do backup de produção (~21 MB) num banco local descartável** antes de subir; as que **não mexem no banco** sobem direto por PR validado. O backup automático (0.1) é a rede final. Reavaliar um staging dedicado se/quando as atualizações grandes de biblioteca (2.x) exigirem.
- [ ] **0.3 Consertar a suíte de testes (~149 quebrados)** — `G` — 🔄 **EM ANDAMENTO.** *Hoje 1090 testes passam e ~149 estão quebrados por causas variadas (isolamento entre testes, tabelas faltando no ambiente de teste, testes desatualizados).* Projeto dedicado, feito por clusters. **Progresso:** os 2 arquivos de estoque que não carregavam foram consertados (PR #317, +14 testes) → ~147 restantes; rollback legado do `tenant_safe_sql` corrigido (validação RLS não precisa mais excluir esse teste). Feito quando: a suíte completa roda verde.
- [ ] **0.4 CI rodar a suíte COMPLETA + travar merge se quebrar + escâner de segurança** — `M` — *Hoje só roda 6% dos testes e não trava nada.* Depende do 0.3. Inclui CodeQL/pip-audit/Trivy (procuram falhas no código e nas bibliotecas). Feito quando: PR com teste quebrado ou falha de segurança não consegue ser mergeado.
- [ ] **0.5 Lint/format automático bloqueante** — `P` — *Padroniza o código e pega erros bobos antes.* (ruff/black no backend, ESLint/Prettier no frontend.)

---

### FASE 1 — Segurança (fechar os buracos restantes)
*Maior risco ao negócio. Algumas partes dependem do staging (0.2) por mexerem no banco.*

- [x] **1.1 Proteção automática de dados entre lojas — trava + 13 tabelas** — ✅ FEITO (PRs #304, #307, #306, #310). Reduziu de 57 para 44 tabelas expostas.
- [x] **1.2 Proteger as tabelas restantes (catraca zerada fora das globais intencionais)** — ✅ **FEITO** (PRs #319, #322, #325, #331, #335, #337, #339, #341 e #342). O registry multi-tenant não tem mais dívida real: sobraram só 6 tabelas globais de propósito (`user_sessions`, `ops_alerts`, `ops_error_events`, `bling_pedido_webhook_events`, `campaign_event_queue`, `notification_queue`) documentadas/whitelistadas.
  - [x] 1.2a As "fáceis" que sobraram — ✅ **FEITO E NO AR** (PR #319, deploy 09/06): 6 modelos de campanha/fidelidade via TenantScoped, sem mudar o banco → expostas **44 → 38**.
  - [x] 1.2b As de tipo errado: `tenant_id` como texto → converter. ✅ **FEITO** (PR #322: `data_subject_requests`, `assinaturas_modulos`, `ecommerce_notify_requests`; PR #325: `pedidos`, `pedido_itens` + ajuste de contexto no checkout; PRs #341/#342 fecharam as 11 do WhatsApp com `tenant_id` UUID + TenantScoped).
  - [x] 1.2c As de tipo errado: `tenant_id` como número (~8, ex. IA/fiscal) → converter. ✅ **FEITO** na consolidação fiscal/IA (PRs #328/#329/#330); confirmado pela catraca sem dívida real fora das globais intencionais.
  - [x] 1.2d As "campaigns" → proteção automática (TenantScoped). ✅ **FEITO E NO AR** (PR #335: `campaign_executions`, `coupon_redemptions`; **PR #337**: as 7 restantes — `campaigns`, `cashback_transactions`, `coupons`, `drawings`, `drawing_entries` protegidas + as 2 filas `campaign_event_queue`/`notification_queue` viram "globais de propósito". O robô agendador foi arrumado para trabalhar uma loja por vez. **Sem mexer no banco.**)
  - [x] 1.2e As que aceitam loja "em branco" → preencher e travar. ✅ **comissões** (`comissoes_itens`) feito (PR #331). ✅ **DRE** (`dre_periodos`) **FECHADO E NO AR** (PR #339, deploy 10/06): 13 telas/relatórios do DRE passaram a usar a loja do contexto; a tabela virou protegida (automática) + travada (loja obrigatória, migration `pn20260610a1`; tabela vazia em prod = **sem mexer em dados**). A escrita passou a gravar a loja **ativa** (corrige um caso de usuário que trabalha em mais de uma loja). 6 revisores céticos, **0 furos**.
  - [x] 1.2f `template` (cadastro de nova loja) — ✅ **FEITO E NO AR** (PR #335, deploy 10/06): as 2 tabelas (`tenant_template_installs`, `tenant_template_item_installs`) protegidas; o onboarding passou a definir a loja no contexto (cobre signup, CLI e testes), **sem mexer no banco**.
  - [x] 1.2g 3 tabelas "de sistema" declaradas globais de propósito — ✅ **FEITO** (PR 09/06): `ops_alerts`, `ops_error_events` (central de erros/alertas que só o admin vê, cross-tenant de propósito) e `bling_pedido_webhook_events` (fila do robô do Bling) não guardam dados de loja → marcadas como intencionalmente globais (catraca + whitelist do filtro), **sem mexer no banco** → dívida real **31 → 28**.
- [ ] **1.3 RLS no banco (defesa final)** — `M` — *Trava no próprio PostgreSQL: mesmo que algum código esqueça, o banco se recusa a entregar dado de outra loja.* A última linha de defesa. 🔄 **Em levas:** ponte de contexto RLS no backend pronta; canary de policy nas tabelas de template preparada; expansões em cadastros-base, catálogo de produtos, opções de ração, segmentação de clientes, configurações fiscais de produto, campanhas (`núcleo`, recompensas, sorteios, históricos/logs), campanha por validade e configurações de loja preparadas; depois expandir por blocos de baixo risco.
- [x] **1.4 Chave de criptografia de pagamento (Mercado Pago) obrigatória no boot** — ✅ **FEITO** (PR #316, em produção e validado). Removido o "atalho inseguro" (chave de login + literal embutido no código); a chave dedicada virou **obrigatória** (boot aborta se faltar). Em produção foi definida com o valor efetivo anterior, **sem recriptografar** — a configuração de pagamento existente segue intacta, e as duas chaves ficaram independentes.
- [x] **1.5 Remover a integração Stone (que vai ser descontinuada)** — `M` — ✅ **FEITO**: rota online `/stone`, tela de configuração, wrapper POS e script de consentimento removidos; migration `pq20260611a1` desativa `stone_configs` e limpa credenciais em texto plano. Histórico/tabelas antigas ficam preservados para auditoria, e a conciliação por planilha segue fora desta remoção.
- [x] **1.6 Login mais seguro: token curto + renovação automática** — ✅ **FEITO** (PR #346): access token reduzido para 15 minutos, refresh token atrelado à sessão/JTI, endpoint `/auth/refresh` e renovação automática no frontend.
- [x] **1.7 Permissões de administrador por loja** — ✅ **FEITO** (PR #347): dono da loja não nasce mais como admin global; permissões de admin vêm de `UserTenant -> Role -> RolePermission` no tenant selecionado.
- [x] **1.8 Conferência de banco no boot (reativar)** — ✅ **FEITO** (PR #344): `ensure_db_ready(engine)` voltou ao `on_startup()`; banco sem `alembic_version`/head esperado bloqueia o boot antes de aceitar tráfego.
- [x] **1.9 Corrigir vazamento no DRE/Simples entre lojas** — ✅ **FEITO E NO AR** (PR #320, deploy 09/06). *Quatro consultas buscavam o período do DRE só por mês/ano (sem a loja) → uma loja podia ver e até **alterar** o relatório financeiro de outra.* Corrigido com filtro por loja, sem mexer no banco. **Achado durante a diligência das 44 tabelas.** Causa de fundo (a migração multi-loja do DRE está pela metade) fica para um trabalho dedicado.

---

### FASE 2 — Atualizar bibliotecas (modernização)
*Reduz risco de segurança e dívida. Um upgrade por vez, validado pelos testes.*

- [x] **2.1 Corrigir 4 falhas conhecidas + Node atualizado + monitoramento** — ✅ FEITO (PRs #311, #312).
- [ ] **2.1b Padronizar runtime Node 22 para Vite 8** — `P` — 🔄 **EM ANDAMENTO.** Frontend com Vite 8 exige Node `>=20.19.4`/`>=22.12.0`; a próxima fatia alinha CI/Docker/contrato do repo em Node 22 e adiciona trava no deploy antes do `git reset`, para não avançar produção com host em Node 18.
- [ ] **2.2 Atualizar o "núcleo" do backend** — `M` — *FastAPI, Pydantic, SQLAlchemy estão ~2 anos atrás; subir para o atual, com testes.*
- [ ] **2.3 "Trava de versões" (lockfile) + separar bibliotecas** — `P`–`M` — *Garante que todo ambiente use exatamente as mesmas versões; tira bibliotecas pesadas (relatórios/IA) de dentro da imagem do sistema.*
- [ ] **2.4 Atualizar o frontend** — `M` (um de cada vez) — React 19 → React Router 7 → Vite 8 → Tailwind 4 → ícones (lucide, muito antigo).
- [ ] **2.5 Atualizar o app mobile** — `M` — Expo para a versão atual + navegação.

---

### FASE 3 — Arquitetura (organizar o código)
*O maior esforço. Depende dos testes (0.3) para refatorar com segurança.*

- [ ] **3.1 Backend: quebrar os arquivos-gigantes** — `G` — *Arquivos com milhares de linhas (produtos, pedidos, NF-e) viram módulos menores e organizados.* Inclui: 1 sistema de eventos só (hoje tem dois), `main.py` enxuto, modelos num lugar só.
- [ ] **3.2 Frontend: quebrar as telas-gigantes + organizar** — `G` — *Telas de 1500+ linhas viram peças menores; 1 jeito só de falar com o servidor; type-safety (TypeScript) nos fluxos de dinheiro; biblioteca de componentes reutilizáveis.*

---

### FASE 4 — Operação e observabilidade (DevOps profissional)

- [ ] **4.1 Deploy automático com rollback automático** — `M` — *Hoje o deploy é manual por SSH; virar um botão/automação segura, que se desfaz sozinha se a saúde falhar.*
- [ ] **4.2 Alerta de erros (Sentry) + métricas + alertas acionáveis** — `M` — *Hoje, se algo quebra em produção, ninguém é avisado na hora. Passar a ser avisado.*
- [ ] **4.3 Limites de recursos + banco gerenciado** — `M` — *Evita que um problema derrube a máquina inteira; banco com backup nativo do provedor.*
- [ ] **4.4 Segredos em cofre** — `P`–`M` — *Senhas e chaves saem de arquivos de texto para um gerenciador seguro.*

---

### FASE 5 — Produto (frontend, mobile, fiscal)

- [ ] **5.1 Testes automáticos no frontend** — `M` — *Começar pelos fluxos que mexem com dinheiro (PDV, pagamento, conciliação).*
- [ ] **5.2 Cobrir o fiscal com testes** — `M` — *NF-e e impostos são área de risco regulatório com pouca cobertura hoje.*
- [ ] **5.3 Mobile: rastreio em background + alerta de erros** — `M`.
- [ ] **5.4 Modernização de telas** — `G` — conforme prioridade do produto.

---

## Itens "spin-off" já abertos (em paralelo)
- [x] Corrigir/limpar o seed de adquirentes quebrado (chip). ✅ **FEITO** (Levas 19/20: seed/admin passa pelo tenant selecionado e grava na tabela real `templates_adquirentes`).
- [x] Deduplicar modelos fiscais + corrigir tipo do `tenant_id` (chip). ✅ **FEITO** (PRs #328/#329/#330; pré-requisito do 1.2c fechado).

---

## Critério de "10/10 real" por área (quando declarar fechado)
- **Segurança:** 100% das tabelas com isolamento automático + RLS no banco; zero segredo em texto plano; token curto + renovação; permissões por loja.
- **DevOps:** deploy automático com rollback; staging; alertas na hora; limites de recurso; backup testado.
- **Bibliotecas:** no máximo 1 versão "major" atrás em tudo; zero falha de segurança conhecida; lockfile.
- **Arquitetura:** nenhum arquivo "gigante"; 1 padrão por camada; sem código duplicado.
- **Testes:** suíte completa rodando no CI e travando merge; cobertura alta nos fluxos de dinheiro/fiscal.

---

## Ordem recomendada para começar AGORA

1. ✅ **1.4** (chave de pagamento) — FEITO (PR #316).
2. **0.2** — ✅ **DECIDIDO (09/06): sem staging separado; mudanças de banco testadas com cópia local do backup de produção.** Destravou as levas de isolamento e segue valendo para mudanças de banco.
3. **0.3** (consertar testes) — 🔄 em andamento, sem custo, no lado do assistente.
4. Depois: **1.3** (RLS), **0.4** (CI) e **0.5** (lint), em paralelo com **2.x** (atualizações).

## Log de execução

### 2026-06-08
PRs concluídos e mergeados na `main` (os de produção já no ar e validados):

- #304 — catraca multi-tenant (teste anti-regressão)
- #307 — conciliação: 5 modelos protegidos
- #306 — comissões: ComissaoVenda protegido
- #310 — opções de ração + duplicatas: 7 modelos protegidos → expostos **57 → 44**
- #309 — backup automático (pg_dump) antes das migrations
- #311 — correção de 4 falhas de segurança (CVEs) + Node 18→20
- #312 — Dependabot cobrindo backend e frontend
- #315 — este guia
- #316 — chave de pagamento dedicada e obrigatória (**item 1.4**)
- #317 — conserta 2 testes de estoque (**início do 0.3**)

Deploys de produção no dia: **4** (todos validados, sistema saudável). Backup automático **validado em produção**. Placar geral: ~5,1 → ~5,3/10.

### 2026-06-09
- **Decisão (Lucas):** sem servidor de staging separado — mudanças de banco serão testadas com **cópia local do backup de produção** (~21 MB); as fáceis sobem direto. (Item **0.2**.)
- **Diligência multi-agente** das 44 tabelas restantes: mapa verificado por risco (fáceis / converter tipo / preencher branco / robôs em segundo plano). Guardado na memória do projeto.
- #319 — Leva 1: 6 modelos de campanha/fidelidade protegidos (TenantScoped, sem migração) → expostas **44 → 38**. (Item **1.2a**.)
- #320 — **vazamento de dados entre lojas no DRE/Simples corrigido** (4 consultas buscavam o período só por mês/ano). (Item **1.9**.) Achado durante a diligência.
- #322 — Leva 2: 3 tabelas de tipo texto convertidas para UUID e protegidas (`data_subject_requests`, `assinaturas_modulos`, `ecommerce_notify_requests`) → expostas **38 → 35**. (Item **1.2b**, parcial.) Validado por checagem só-leitura na produção antes de subir; **0 fail-fast** nos logs depois.
- 🔎 **Achado (tarefa à parte):** a tabela `simples_nacional_mensal` **não existe em produção** (o modelo nunca teve migração de criação) → a funcionalidade do Simples mensal provavelmente está quebrada lá. Removida desta leva; chip de tarefa aberto pra criar a tabela.
- #325 — Leva 3: `pedidos` e `pedido_itens` (coração das vendas) convertidos para UUID e protegidos → expostas **35 → 33**. (Item **1.2b**.) A auditoria do checkout achou 1 ponto sem a loja no contexto → corrigido no mesmo PR. Validado em prod; **0 fail-fast** depois. (Colisão de migração com a outra sessão detectada e resolvida.)
- #324 — **mergeado E no ar** (outra sessão, deploy 09/06): criou em produção a tabela `simples_nacional_mensal` (que não existia) + `tenant_id` UUID/TenantScoped. Head do Alembic = `pi20260609a1` (fila única conferida — sem "duas cabeças"); pós-deploy containers healthy e **0 fail-fast**.
- ✅ **Globais de propósito**: `ops_alerts`, `ops_error_events` e `bling_pedido_webhook_events` declaradas intencionalmente globais (catraca + whitelist do filtro), **sem mudança de banco**. Diligência leu o código: são observabilidade do admin (cross-tenant) e fila de worker do Bling (claim sem contexto) — isolar por loja quebraria o painel e o robô. Pela catraca, a dívida real cai **31 → 28** (o "33" do log do #325 já estava defasado: a baseline não conta `simples_nacional_mensal` desde o #324). 127/127 testes multi-tenant + 6/6 de ops verdes. (Item **1.2g**.)

Deploys de produção no dia: **4** (#319+#320, #322, #325, #324 — todos com backup automático antes; sistema saudável, **0 fail-fast** nos logs). As "globais de propósito" (1.2g) ainda **não** foram deployadas — são só catraca/whitelist, sem mudança de banco. Placar geral: ~5,3 → ~5,5/10.

### 2026-06-10
- (outra sessão) **#328/#329/#330 mergeados e no ar**: #328 corrige atributos de alíquota do Simples; #329 **consolida o fiscal** (kit/produto/variacao/empresa_config_fiscal + TenantScoped, remove as cópias duplicadas); #330 `configuracoes_custo_moto` texto→UUID + TenantScoped. → dívida **28 → 24**.
- #331 — **comissões (`comissoes_itens`)**: preenche a loja via venda + tranca (NOT NULL) + proteção automática (TenantScoped); migration `pm20260609a1`. Checagem só-leitura em prod: 3 linhas, todas já com loja, 0 órfãs → backfill é no-op. → dívida **24 → 23**. (Item **1.2e**, parcial.) Mergeado e **no ar** (deploy 2026-06-10).
- #332 — **DRE (`dre_periodos`) — tampa furo de escrita**: os 4 pontos que criavam o período só com usuário agora gravam a loja (`tenant_id`). **Não** trancou a tabela (segue na baseline) — etapa intermediária segura (várias leituras rodam sem a loja no contexto). Só código; mergeado e **no ar** (deploy 2026-06-10). Tabela vazia em prod.
- #335 — **template (2) + campanhas soltas (2)**: `tenant_template_installs`, `tenant_template_item_installs`, `campaign_executions`, `coupon_redemptions` protegidas (TenantScoped, **sem banco**). O onboarding passou a definir a loja no contexto (cobre signup, CLI e testes); os 2 backfills de campanha viraram tenant-safe. Verificação adversarial (2 revisores) + 212 testes verdes; read-only prod: 4 tabelas, 0 linhas sem loja. → dívida **23 → 19**. (Itens **1.2f** e **1.2d** parcial.) Mergeado e **no ar** (deploy 2026-06-10).
- #337 — **campanhas (as 7 restantes)**: `campaigns`, `cashback_transactions`, `coupons`, `drawings`, `drawing_entries` protegidas (TenantScoped, Option B) + `campaign_event_queue`/`notification_queue` viram "globais de propósito" (filas de robô, claim entre lojas). **Pré-requisito no mesmo PR:** o robô agendador (`scheduler.py`) passou a trabalhar uma loja por vez nas 4 tarefas (os "sorteios automáticos" agora varrem loja por loja; "destaque do mês"/"criar campanhas padrão"/"expirar cashback" definem a loja por iteração). Diligência completa + verificação adversarial (6 revisores céticos, **0 furos**). **Sem mexer no banco.** → dívida **19 → 12** (Item **1.2d** ✅, **1.2 hoje 12**). Mergeado e **no ar** (deploy 2026-06-10).
- #339 — **DRE (`dre_periodos`)**: fecha o isolamento do DRE (que estava pela metade). 13 telas/relatórios passaram a usar a loja do contexto; a tabela virou protegida (TenantScoped) + travada (loja obrigatória, migration `pn20260610a1`). **Mexe no banco**, mas a tabela está **vazia** em prod → no-op de dados (pós-deploy: `tenant_id` NOT NULL confirmado). A escrita passou a gravar a loja **ativa** (corrige caso de usuário multi-loja — achado dos 6 revisores céticos, **0 furos**). → dívida **12 → 11** (Item **1.2e** ✅). Mergeado e **no ar** (deploy 2026-06-10). 🔎 Achado pré-existente (tarefa à parte): 5 outras telas do DRE com erro 500 antigo, sem relação com esta mudança.

Head do Alembic = `pm20260609a1` (fila única conferida — sem "duas cabeças"). **#331, #332 e #333 deployados em produção (2026-06-10, autorizado pelo Lucas)**: backup automático antes (`deploy_20260610_095754`), migração `pm20260609a1` aplicada, `comissoes_itens` 3/0 (NOT NULL ok), **0 `[ORM FAIL-FAST]`** nos logs (os 36 `RuntimeError: No response returned` eram o transitório benigno da janela de restart, em `/produtos/vendaveis`; já cessou), containers healthy + tráfego real (vendas/estoque/Bling NF 200). Em seguida o **#335 também subiu** (deploy `deploy_20260610_105904`): 4 tabelas protegidas, **sem banco**, **0 `[ORM FAIL-FAST]`**, healthy, servindo normal. Em seguida o **#337** também subiu (deploy `deploy_20260610_142214`): campanhas protegidas + robô agendador escopado por loja, **sem banco**, **0 `[ORM FAIL-FAST]`**, **0 RuntimeError**, scheduler iniciou limpo, 4 containers healthy. Por fim o **#339** subiu (deploy `deploy_20260610_154451`, **mexeu no banco**): migração `pn20260610a1` aplicada (head agora `pn20260610a1`), `dre_periodos.tenant_id` agora **NOT NULL** (tabela vazia → no-op de dados), **0 `[ORM FAIL-FAST]`**, **0 RuntimeError**, 4 containers healthy. **Dívida 19 → 11** (de 31 no começo do dia — restam só as 11 do WhatsApp). Placar geral: ~5,6/10.

### 2026-06-11
- #341 — **WhatsApp Levas 1 e 2**: `whatsapp_agents`, `whatsapp_handoffs`, `whatsapp_internal_notes`, `tenant_whatsapp_config`, `whatsapp_ia_sessions`, `whatsapp_ia_messages` e `whatsapp_ia_metrics` migradas para `TenantScoped`/UUID obrigatório. → dívida **11 → 4**. (Item **1.2b**.)
- #342 — **WhatsApp Leva 3 (LGPD/security)**: `data_privacy_consents`, `data_access_logs`, `data_deletion_requests` e `security_audit_logs` migradas para `TenantScoped`/UUID obrigatório. → dívida **4 → 0** fora das globais intencionais. (Item **1.2b** ✅; **1.2** ✅.)
- #343 — **DRE com contexto tenant nas rotas multi-tenant**: correção complementar para rotas do DRE usarem a loja ativa corretamente. (Endurecimento do item **1.2e**.)
- #344 — **conferência de banco no boot reativada**: `ensure_db_ready(engine)` voltou ao `on_startup()` e bloqueia o backend se `alembic_version`/head esperado não bater. Validação: teste vermelho/verde do guard no startup, `test_phase2a_startup_db_alembic.py` com 11 testes verdes, compileall, `git diff --check`, Alembic com 1 head e fluxo único. (Item **1.8** ✅.)
- #345 — **integração Stone online removida**: rota online `/stone`, tela de configuração, wrapper POS e script de consentimento saíram; migration `pq20260611a1` desativa `stone_configs` e limpa credenciais em texto plano. (Item **1.5** ✅.)
- #346 — **login com token curto e refresh automático**: access token de 15 minutos, refresh token por sessão/JTI, `/auth/refresh`, bloqueio de refresh token como bearer e renovação automática no frontend. Validação backend/frontend/build verde. (Item **1.6** ✅.)
- #347 — **admin por loja**: o dono da loja não nasce mais com `is_admin=True` global; permissões de administração passam a vir da role/permissão do tenant selecionado. CI completa verde: Tests & Quality, Migration Smoke, Smoke, MCP, Fluxo único, Quality Gate e SonarCloud. (Item **1.7** ✅.)

Estado atual da catraca multi-tenant: **0 tabela com dívida real**. Restam expostas apenas as 6 globais de propósito, documentadas na whitelist (`user_sessions`, `ops_alerts`, `ops_error_events`, `bling_pedido_webhook_events`, `campaign_event_queue`, `notification_queue`).

- RLS **Leva 1** — backend preparado para sincronizar o tenant atual com uma variável transacional do PostgreSQL (`app.tenant_id`) em ORM, flush e SQL cru tenant-safe. **Ainda não habilita policy no banco**; a próxima leva cria a migration/policies. (Item **1.3**, parcial.)
- RLS **Leva 2 canária** — migration `pr20260611a1` habilita/força RLS em `tenant_template_installs` e `tenant_template_item_installs`, com policy `USING` + `WITH CHECK` baseada em `current_setting('app.tenant_id', true)`. Escopo pequeno para validar o padrão antes de expandir. (Item **1.3**, parcial.)
- RLS **Leva 3 cadastros-base** — migration `ps20260611a1` habilita/força RLS em `formas_pagamento`, `especies` e `racas`, mantendo a mesma policy `USING` + `WITH CHECK` baseada em `current_setting('app.tenant_id', true)`. Escopo ainda pequeno, focado em tabelas de onboarding já protegidas pelo filtro automático. (Item **1.3**, parcial.)
- RLS **Leva 4 catálogo de produtos** — migration `pt20260611a1` habilita/força RLS em `departamentos`, `marcas` e `categorias`, com a mesma policy de tenant transacional. Escopo focado em cadastros de produto já `BaseTenantModel` e usados nos fluxos de onboarding/importação de catálogo. (Item **1.3**, parcial.)
- RLS **Leva 5 opções de ração** — migration `pu20260611a1` habilita/força RLS em `linhas_racao`, `portes_animal`, `fases_publico`, `tipos_tratamento`, `sabores_proteina` e `apresentacoes_peso`, mantendo a policy transacional por loja. Escopo focado em tabelas auxiliares já `TenantScoped` e usadas no catálogo/importação de rações. (Item **1.3**, parcial.)
- RLS **Leva 6 segmentação de clientes** — migration `pv20260611a1` habilita/força RLS em `cliente_segmentos`, tabela pequena e já filtrada por `tenant_id` nas rotas e no dashboard gerencial. (Item **1.3**, parcial.)
- RLS **Leva 7 configurações fiscais de produto** — migration `pw20260611a1` habilita/força RLS em `produto_config_fiscal`, `kit_config_fiscal` e `variacao_config_fiscal`, tabelas canônicas já `TenantScoped` e validadas pela consolidação fiscal. (Item **1.3**, parcial.)
- RLS **Leva 8 campanhas núcleo** — migration `px20260611a1` habilita/força RLS em `campaigns`, `campaign_executions`, `campaign_run_log` e `campaign_locks`, sem tocar nas filas globais de propósito (`campaign_event_queue`/`notification_queue`). (Item **1.3**, parcial.)
- RLS **Leva 9 recompensas de campanhas** — migration `py20260611a1` habilita/força RLS em `loyalty_stamps`, `cashback_transactions`, `coupons` e `coupon_redemptions`, mantendo as filas globais fora do escopo. (Item **1.3**, parcial.)
- RLS **Leva 10 sorteios de campanhas** — migration `pz20260611a1` habilita/força RLS em `drawings` e `drawing_entries`, cobrindo sorteios auditáveis e seus participantes sem tocar nas filas globais. (Item **1.3**, parcial.)
- RLS **Leva 11 históricos/logs de campanhas** — migration `qa20260611a1` habilita/força RLS em `customer_rank_history`, `notification_log` e `customer_merge_logs`, fechando os históricos tenant-scoped restantes do módulo de campanhas. (Item **1.3**, parcial.)
- RLS **Leva 12 campanha por validade** — migration `qb20260611a1` habilita/força RLS em `campanha_validade_automatica` e `campanha_validade_exclusoes`, mantendo o escopo nos ajustes de promoção por validade. (Item **1.3**, parcial.)
- RLS **Leva 13 configurações de loja** — migration `qc20260611a1` habilita/força RLS em `empresa_config_geral` e `configuracoes_custo_moto`, mantendo fora desta leva `configuracoes_entrega` por ter uma leitura SQL direta no checkout que precisava de ajuste próprio antes da policy. (Item **1.3**, parcial.)
- RLS **Leva 14 configurações de entrega** — checkout deixou de ler `configuracoes_entrega` via SQL cru e passou a usar `ConfiguracaoEntrega` com filtro de tenant/ponte ORM; migration `qd20260611a1` habilita/força RLS em `configuracoes_entrega` com a mesma policy transacional por loja. (Item **1.3**, parcial.)
- RLS **Leva 15 WhatsApp** — migration `qf20260611a1` habilita/força RLS nas 11 tabelas tenant-scoped do WhatsApp (`tenant_whatsapp_config`, sessões/mensagens/métricas IA, handoff/atendentes/notas internas e LGPD/security), mantendo o padrão `app.tenant_id` transacional. (Item **1.3**, parcial.)
- RLS **Leva 16 fiscal complementar** — migration `qg20260611a1` habilita/força RLS em `empresa_config_fiscal` e `simples_nacional_mensal`, cobrindo configuração fiscal da empresa e histórico mensal do Simples com a mesma policy transacional por loja. (Item **1.3**, parcial.)
- RLS **Leva 17 conciliação/históricos** — migration `qh20260611a1` habilita/força RLS em `conciliacao_validacoes`, `conciliacao_logs`, `conciliacao_metricas` e `historico_conciliacao`, mantendo fora desta leva os templates/admin seed antigos para ajuste próprio de contexto. (Item **1.3**, parcial.)
- RLS **Leva 18 conciliação core** — migration `qi20260611a1` habilita/força RLS em `empresa_parametros`, `arquivos_evidencia`, `conciliacao_importacoes`, `conciliacao_lotes` e `conciliacao_recebimentos`, mantendo `adquirentes_templates` para uma leva própria por causa do seed/admin legado. (Item **1.3**, parcial.)
- RLS **Leva 19 templates de adquirentes** — rota admin de seed passa a usar `get_current_user_and_tenant`, o seed foi alinhado ao schema atual (`nome` + `tipo_arquivo`) e a migration `qj20260611a1` habilita/força RLS em `adquirentes_templates`. (Item **1.3**, parcial.)
- RLS **Leva 20 templates de adquirentes legado** — pós-deploy mostrou que produção tem a tabela legado `templates_adquirentes` (e não `adquirentes_templates`); o seed passa a gravar no modelo/tabela real e a migration `qk20260611a1` habilita/força RLS em `templates_adquirentes`. (Item **1.3**, parcial.)
- RLS **Leva 21 conciliação bancária legado** — migration `ql20260612a1` habilita/força RLS em `extratos_bancarios`, `movimentacoes_bancarias`, `regras_conciliacao` e `provisoes_automaticas`, cobrindo o fluxo OFX/conciliação bancária legado já dependente de `get_current_user_and_tenant`. (Item **1.3**, parcial.)
- RLS **Leva 22 financeiro-base** — migration `qm20260612a1` habilita/força RLS em `contas_bancarias`, `categorias_financeiras`, `tipo_despesas`, `formas_pagamento_taxas` e `configuracao_impostos`, cobrindo cadastros financeiros usados por rotas com loja ativa e onboarding via SQL tenant-safe. (Item **1.3**, parcial.)
- RLS **Leva 23 lançamentos financeiros** — migration `qn20260612a1` habilita/força RLS em `movimentacoes_financeiras`, `lancamentos_manuais` e `lancamentos_recorrentes`, cobrindo extrato unificado e lançamentos manuais/recorrentes que já rodam com loja ativa nas rotas. (Item **1.3**, parcial.)
- RLS **Leva 24 contas financeiras** — migration `qo20260612a1` habilita/força RLS em `contas_pagar`, `contas_receber`, `pagamentos` e `recebimentos`, cobrindo títulos financeiros e baixas que já passam pelo contexto da loja ou por SQL tenant-safe nos fluxos principais. (Item **1.3**, parcial.)
- RLS **Leva 25 caixas** — migration `qp20260612a1` habilita/força RLS em `caixas` e `movimentacoes_caixa`, cobrindo abertura/fechamento de caixa e movimentações registradas pelo PDV com loja ativa. (Item **1.3**, parcial.)
- RLS **Leva 26 pedidos e-commerce** — migration `qq20260612a1` habilita/força RLS em `pedidos` e `pedido_itens`, cobrindo checkout/e-commerce já protegido por contexto de loja nas rotas públicas, carrinho, webhooks e serviço de checkout. (Item **1.3**, parcial.)
- RLS **Leva 27 LGPD/avise-me** — migration `qr20260612a1` habilita/força RLS em `data_subject_requests` e `ecommerce_notify_requests`, deixando `assinaturas_modulos` para uma leva própria porque a ativação admin por tenant alvo precisa preparar contexto antes da policy. (Item **1.3**, parcial.)
- RLS **Leva 28 assinaturas de módulos** — ativação admin de módulo passa a estabelecer o contexto do tenant alvo antes de consultar/criar `assinaturas_modulos`; migration `qs20260612a1` habilita/força RLS nessa tabela. (Item **1.3**, parcial.)
- RLS **Leva 29 plano de contas do DRE** — migration `qt20260612a1` habilita/força RLS em `dre_categorias`, `dre_subcategorias`, `regras_classificacao_dre` e `historico_classificacao_dre`, cobrindo configurações e histórico de classificação já tenant-scoped. (Item **1.3**, parcial.)
- RLS **Leva 30 validade de estoque** — migration `qu20260612a1` habilita/força RLS em `estoque_validade_bloqueios`, cobrindo bloqueios operacionais de lotes por validade usados por rotas e serviço ORM com loja ativa. (Item **1.3**, parcial.)
- RLS **Leva 31 pendências de compras** — migration `qv20260612a1` habilita/força RLS em `compras_pendencias_fornecedor`, `compras_pendencias_fornecedor_itens` e `compras_pendencias_fornecedor_historico`, cobrindo o workflow de divergência de fornecedor já tenant-scoped. (Item **1.3**, parcial.)
- RLS **Leva 32 grupos de fornecedores** — migration `qw20260612a1` habilita/força RLS em `fornecedor_grupos`, cobrindo o cadastro comercial de grupos de fornecedores já filtrado por loja nas rotas e no merge de pessoas. (Item **1.3**, parcial.)
- RLS **Leva 33 histórico Stone** — migration `qx20260612a1` habilita/força RLS em `stone_transactions`, `stone_transaction_logs` e `stone_configs`, preservando histórico/auditoria da integração online já retirada sem manter essas tabelas fora da defesa final do banco. (Item **1.3**, parcial.)
- RLS **Leva 34 duplicatas ignoradas** — o merge de produtos passou a limpar `duplicatas_ignoradas` via SQL tenant-safe com contexto RLS sincronizado; migration `qy20260612a1` habilita/força RLS nessa tabela auxiliar de sugestão de rações. (Item **1.3**, parcial.)
- RLS **Leva 35 histórico de crédito de clientes** — migration `qz20260612a1` habilita/força RLS em `credito_logs`, cobrindo o extrato de movimentações de crédito já gravado e lido pelo cadastro do cliente com loja explícita. (Item **1.3**, parcial.)
- RLS **Leva 36 feature flags** — o serviço de feature flags passou a sincronizar o contexto RLS usando o `tenant_id` explícito antes de consultar/criar/listar flags; migration `ra20260612a1` habilita/força RLS em `feature_flags`. (Item **1.3**, parcial.)
- RLS **Leva 37 operadoras de cartão** — a validação de parcelas na finalização de venda passou a buscar `operadoras_cartao` por `id` e `tenant_id` explícitos; migration `rb20260612a1` habilita/força RLS nessa tabela de cadastro de operadoras. (Item **1.3**, parcial.)
- RLS **Leva 38 pendências de estoque** — os serviços de notificação/finalização de lista de espera passaram a sincronizar o contexto RLS com `tenant_id` explícito; migration `rc20260612a1` habilita/força RLS em `pendencias_estoque`. (Item **1.3**, parcial.)
- RLS **Leva 39 alertas de estoque negativo** — o serviço de estoque passou a sincronizar o contexto RLS antes de validar/criar alertas de estoque negativo; migration `rd20260612a1` habilita/força RLS em `alertas_estoque_negativo`. (Item **1.3**, parcial.)
- RLS **Leva 40 cargos de RH** — provisões trabalhistas/benefícios passaram a filtrar `Cargo.tenant_id` explicitamente nos joins de funcionários; migration `re20260612a1` habilita/força RLS em `cargos`. (Item **1.3**, parcial.)
- RLS **Leva 41 RBAC** — migration `rf20260612a1` habilita/força RLS em `roles` e `role_permissions`; `user_tenants` permanece fora por ser necessário no fluxo pré-seleção de tenant. (Item **1.3**, parcial.)
- RLS **Leva 42 lembretes** — a recorrência automática de vendas passa a consultar/criar `lembretes` com `tenant_id` explícito; migration `rg20260612a1` habilita/força RLS em `lembretes`. (Item **1.3**, parcial.)
- RLS **Leva 43 comissões consolidadas** — migration `rh20260612a1` habilita/força RLS em `comissoes_vendas`; `comissoes_itens` fica para uma leva própria porque ainda há rota avançada com SQL cru legado a preparar. (Item **1.3**, parcial.)
- RLS **Leva 44 itens de comissões** — rotas avançadas/diagnóstico/relatórios de comissões deixam de ler/atualizar `comissoes_itens` por SQL cru direto e passam a sincronizar o contexto tenant-safe; migration `ri20260612a1` habilita/força RLS em `comissoes_itens`. (Item **1.3**, parcial.)
- RLS **Leva 45 oportunidades do PDV** — serviços auxiliares de oportunidades que abrem `SessionLocal` passam a ativar/limpar o tenant antes de consultar/gravar eventos; migration `rj20260612a1` habilita/força RLS em `opportunities` e `opportunity_events`. (Item **1.3**, parcial.)
- RLS **Leva 46 catálogos veterinários** — seed veterinário já executa com contexto por tenant e rotas de catálogo/agenda usam ORM com loja ativa; migration `rk20260612a1` habilita/força RLS em `vet_medicamentos_catalogo`, `vet_protocolos_vacinas`, `vet_catalogo_procedimentos` e `vet_consultorios`. (Item **1.3**, parcial.)
- RLS **Leva 47 agenda veterinária** — rotas de agenda/consulta veterinária já passam pela loja ativa antes das leituras e escritas ORM; migration `rl20260612a1` habilita/força RLS em `vet_agendamentos` e `vet_consultas`. (Item **1.3**, parcial.)
- RLS **Leva 48 prontuário veterinário** — registros clínicos vinculados à consulta usam ORM com `tenant_id` explícito nas rotas de consultas, exames, relatórios e app mobile; migration `rm20260612a1` habilita/força RLS em `vet_prescricoes`, `vet_itens_prescricao`, `vet_vacinas_registros`, `vet_exames` e `vet_procedimentos_consulta`. (Item **1.3**, parcial.)
- RLS **Leva 49 acompanhamento veterinário do pet** — rotas de acompanhamento e consulta usam a loja ativa e filtros explícitos para peso, fotos clínicas e perfil comportamental; migration `rn20260612a1` habilita/força RLS em `vet_peso_registros`, `vet_fotos_clinicas` e `vet_perfil_comportamental`. (Item **1.3**, parcial.)
- RLS **Leva 50 internação veterinária** — rotas de internação, app veterinário, extratos e relatórios usam ORM com loja ativa/contexto transacional antes de consultar ou gravar internações, evoluções, configurações e agenda operacional; migration `ro20260612a1` habilita/força RLS em `vet_internacoes`, `vet_internacao_configuracoes`, `vet_evolucoes_internacao` e `vet_internacao_procedimentos_agenda`. (Item **1.3**, parcial.)
- RLS **Leva 51 orçamentos veterinários** — rotas de orçamento veterinário leem e gravam orçamentos/itens com a loja ativa e filtro explícito de `tenant_id`; migration `rp20260612a1` habilita/força RLS em `vet_orcamentos` e `vet_orcamento_itens`. (Item **1.3**, parcial.)
- RLS **Leva 52 vínculo veterinário entre tenants** — `vet_partner_link` foi tratado separadamente por não ter `tenant_id` único: a leitura fica liberada para o tenant da loja (`empresa_tenant_id`) e para o tenant veterinário parceiro (`vet_tenant_id`), enquanto criação/alteração/remoção ficam restritas ao tenant da loja. Migration `rq20260612a1` habilita/força RLS com policies por comando. (Item **1.3**, parcial.)
- RLS **Leva 53 Banho & Tosa retornos** — primeira fatia pequena do bloco `banho_tosa_*`: `banho_tosa_retorno_templates` usa `BaseTenantModel`, rotas e seed com `tenant_id` explícito e sem SQL cru público; migration `rr20260612a1` habilita/força RLS com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 54 Banho & Tosa parâmetros** — `banho_tosa_configuracoes` e `banho_tosa_parametros_porte` usam `BaseTenantModel`, são acessadas por rotas/helpers com loja ativa e alimentadas por seed com `tenant_id` explícito; migration `rs20260612a1` habilita/força RLS nas duas tabelas. (Item **1.3**, parcial.)
- RLS **Leva 55 Banho & Tosa cadastros** — `banho_tosa_recursos`, `banho_tosa_servicos` e `banho_tosa_precos_servico` usam `BaseTenantModel`; rotas, agenda, app e seed consultam/gravam com `tenant_id` explícito, então a migration `rt20260612a1` habilita/força RLS nas três tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 56 Banho & Tosa agenda** — `banho_tosa_agendamentos` e `banho_tosa_agendamento_servicos` usam `BaseTenantModel`; rotas operacionais, app do tutor, capacidade e slots ativam/recebem contexto de tenant e filtram por `tenant_id`, então a migration `ru20260612a1` habilita/força RLS nas duas tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 57 Banho & Tosa atendimento** — `banho_tosa_atendimentos`, `banho_tosa_etapas` e `banho_tosa_fotos` usam `BaseTenantModel`; rotas de atendimento, ocorrências/fotos, app do tutor e helpers operacionais consultam/gravam com loja ativa e `tenant_id` explícito, então a migration `rv20260612a1` habilita/força RLS nas três tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 58 Banho & Tosa insumos** — `banho_tosa_insumos_previstos` e `banho_tosa_insumos_usados` usam `BaseTenantModel`; rotas/helpers de parâmetros e consumo validam atendimento, produto e responsável com loja ativa e `tenant_id` explícito, então a migration `rw20260612a1` habilita/força RLS nas duas tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 59 Banho & Tosa custos** — `banho_tosa_custos_snapshot` e `banho_tosa_taxi_dog` usam `BaseTenantModel`; rotas de custos/táxi dog e recálculo de snapshot consultam/gravam por ORM com loja ativa e filtro explícito de `tenant_id`, então a migration `rx20260612a1` habilita/força RLS nas duas tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 60 Banho & Tosa pacotes** — `banho_tosa_pacotes`, `banho_tosa_pacote_creditos`, `banho_tosa_pacote_movimentos` e `banho_tosa_recorrencias` usam `BaseTenantModel`; rotas/helpers de pacotes, consumo, estorno, recorrências e retornos consultam/gravam por ORM com loja ativa e filtro explícito de `tenant_id`, então a migration `ry20260612a1` habilita/força RLS nas quatro tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 61 Banho & Tosa avaliações** — `banho_tosa_avaliacoes` usa `BaseTenantModel`; métricas, dashboard e relatório operacional consultam por ORM com loja ativa e filtro explícito de `tenant_id`, então a migration `rz20260612a1` habilita/força RLS na tabela com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 62 idempotência operacional** — `idempotency_keys` usa `BaseTenantModel`; o decorator `@idempotent()` agora reconhece as dependências `user_and_tenant`/`current_user_and_tenant` e escopa limpeza, busca e criação da chave por `tenant_id`, então a migration `sa20260612a1` habilita/força RLS na tabela com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 63 rotas de entrega** — `rotas_entrega` e `rotas_entrega_paradas` usam `BaseTenantModel`; rotas administrativas, entregador/e-commerce e serviços de notificação operam com tenant ativo e filtros explícitos de `tenant_id`, e o rastreio mobile passou a filtrar também os SQLs crus de posição GPS por tenant. A migration `sb20260612a1` habilita/força RLS nas duas tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 64 listas de preço** — `listas_preco` e `produto_listas_preco` usam `BaseTenantModel`, aparecem em poucos fluxos do catálogo/merge de produtos e não participam de login, webhook ou scheduler; a migration `sc20260612a1` habilita/força RLS nas duas tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 65 histórico de preços de produtos** — `produtos_historico_precos` usa `BaseTenantModel`, já está no guardrail de SQL tenant-safe e é acessada por rotas/fluxos de notas com loja ativa; a migration `sd20260612a1` habilita/força RLS na tabela com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 66 lotes de produtos** — `produto_lotes` usa `BaseTenantModel`, não é transferida pelo importador de catálogo base e é acessada por rotas/serviços com produto/tenant ativo; a migration `se20260612a1` habilita/força RLS na tabela e adiciona `produto_lotes` ao guardrail de SQL tenant-safe. (Item **1.3**, parcial.)
- RLS **Leva 67 fornecedores de produtos** — `produto_fornecedores` usa `BaseTenantModel`; merges de produto/pessoa e rotas de notas/compras operam com loja ativa e filtros por tenant quando transferem vínculos, então a migration `sf20260612a1` habilita/força RLS na tabela e adiciona `produto_fornecedores` ao guardrail de SQL tenant-safe. (Item **1.3**, parcial.)
- RLS **Leva 68 conversões de granel** — `granel_conversoes` usa `BaseTenantModel`; a rota de conversão grava com loja ativa e as sugestões de compra consultam com filtro explícito de `tenant_id`, então a migration `sg20260612a1` habilita/força RLS na tabela e adiciona `granel_conversoes` ao guardrail de SQL tenant-safe. (Item **1.3**, parcial.)
- RLS **Leva 69 atributos de variação de produtos** — `produtos_atributos`, `produtos_atributos_opcoes` e `produtos_variacoes_atributos` usam `BaseTenantModel`, são acessadas por ORM no gerador de variações e não possuem SQL bruto público; a migration `sh20260613a1` habilita/força RLS nas três tabelas e adiciona o bloco ao guardrail de SQL tenant-safe. (Item **1.3**, parcial.)
- RLS **Leva 70 cache de notas fiscais Bling** — `bling_notas_fiscais_cache` usa `BaseTenantModel`; rotas e serviços principais já filtram por `tenant_id`, os jobs globais de reconciliação descobrem tenants via SQL tenant-safe autorizado e agora ativam temporariamente o contexto do tenant processado antes das consultas ORM. A migration `si20260613a1` habilita/força RLS na tabela e adiciona o cache ao guardrail de SQL tenant-safe. (Item **1.3**, parcial.)
- RLS **Leva 71 integração Bling legado** — `integracao_bling` usa `BaseTenantModel`, guarda configuração/token legado por tenant e não possui rotas/jobs ativos nem SQL cru atual tocando a tabela; por isso não exigiu entrada em `tenant_safe_sql`. A migration `sj20260613a1` habilita/força RLS na tabela com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 72 configurações do sistema** — `configuracoes_sistema` usa `BaseTenantModel`, é uma tabela de configuração por tenant com superfície ativa mínima e sem rotas/jobs/SQL cru atuais tocando diretamente a tabela; por isso não exigiu entrada em `tenant_safe_sql`. A migration `sk20260613a1` habilita/força RLS na tabela com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 73 histórico de atualização do DRE** — `historico_atualizacao_dre` usa `BaseTenantModel`, é histórico auxiliar ligado ao DRE e não possui rota/job/SQL cru atual tocando diretamente a tabela; por isso não exigiu entrada em `tenant_safe_sql`. A migration `sl20260613a1` habilita/força RLS na tabela com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 74 vínculo pedido de compra/nota de entrada** — `pedidos_compra_notas_entrada` usa `BaseTenantModel` e as rotas de compras/notas que consultam ou criam vínculos já filtram por `tenant_id` explícito via ORM; não há SQL cru atual tocando a tabela. A migration `sm20260613a1` habilita/força RLS na tabela com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 75 pedidos de compra** — `pedidos_compra` e `pedidos_compra_itens` usam `BaseTenantModel`; as rotas de compra criam/listam/alteram pedidos com loja ativa e filtros explícitos de `tenant_id`, e o SQL cru do merge de pessoas já adiciona filtro de tenant para `pedidos_compra` quando a coluna existe. A migration `sn20260613a1` habilita/força RLS nas duas tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 76 notas de entrada/SEFAZ** — `notas_entrada` e `notas_entrada_itens` usam `BaseTenantModel`, já estavam no guardrail de SQL tenant-safe e são acessadas pelos fluxos de XML/PDF/compras com loja ativa. Antes da policy, o importador automático da SEFAZ passou a executar dentro de `tenant_context(tenant_id)` para que o scheduler/job sincronize o tenant RLS antes de consultar/criar notas e itens. A migration `so20260613a1` habilita/força RLS nas duas tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 77 pedidos integrados Bling** — `pedidos_integrados` e `pedidos_integrados_itens` usam `BaseTenantModel` e sustentam reservas/reconciliações de pedidos vindos do Bling. Antes da policy, os jobs globais de status e duplicidades passaram a descobrir tenants via `execute_tenant_safe_all(..., require_tenant=False, allow_global=True, global_reason=...)` e a processar cada tenant dentro de `tenant_context(tenant_id)`. A migration `sp20260613a1` habilita/força RLS nas duas tabelas e adiciona ambas aos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 78 sincronização de produtos Bling** — `produto_bling_sync` e `produto_bling_sync_queue` usam `BaseTenantModel` e sustentam vínculo/filas de estoque com o Bling. Antes da policy, os jobs globais de reconciliação recente, reconciliação geral e auto-link passaram a descobrir tenants via SQL tenant-safe autorizado e processar cada loja dentro de `tenant_context(tenant_id)`; as rotas manuais agora passam o tenant selecionado para evitar execução cross-tenant em background. A migration `sq20260613a1` habilita/força RLS nas duas tabelas e adiciona ambas aos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 79 pagamentos e-commerce/Mercado Pago** — `ecommerce_payment_gateway_configs` usa `BaseTenantModel`, guarda credenciais/segredos por loja e é acessada em um ponto pré-tenant: o webhook público precisa descobrir o tenant pelo `webhook_token`. A migration `sr20260613a1` habilita/força RLS na tabela com SELECT normal por `app.tenant_id`, SELECT adicional e estreito por `app.payment_webhook_token` + `provider = 'mercadopago'`, e escrita sempre por tenant; o resolver do webhook passou a sincronizar essa setting transacional e usar `execute_tenant_safe_one(..., require_tenant=False, allow_global=True, global_reason=...)`. A tabela também entrou nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 80 locais de estoque** — `locais_estoque` usa `BaseTenantModel`, representa cadastro operacional de origem/destino de estoque e hoje passa por ORM nas importacoes CSV/PDF e transferencias. A migration `ss20260613a1` habilita/forca RLS na tabela com o guard transacional padrao e adiciona `locais_estoque` aos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 81 monitor Bling flow** — `bling_flow_events` e `bling_flow_incidents` usam `BaseTenantModel` para eventos/incidentes operacionais da integracao Bling. Antes da policy, as escritas e consultas auxiliares do monitor passaram a sincronizar `app.tenant_id` a partir do `tenant_id` explicito recebido pelo servico, cobrindo chamadas via rota e scheduler com sessao propria. A migration `st20260613a1` habilita/forca RLS nas duas tabelas com o guard transacional padrao. (Item **1.3**, parcial.)
- RLS **Leva 82 IA fluxo de caixa** — `fluxo_caixa`, `indices_saude_caixa` e `projecao_fluxo_caixa` usam `BaseTenantModel`; as rotas da ABA 5 entram por `get_current_user_and_tenant` e os serviços internos já resolvem/gravam/consultam com `tenant_id` explícito, então a migration `su20260613a1` habilita/força RLS nas três tabelas com o guard transacional padrão. (Item **1.3**, parcial.)
- RLS **Leva 83 perfis de acesso do app** — `app_access_profiles` usa `BaseTenantModel` e guarda liberacoes de perfil por tenant/usuario/pessoa; a tabela e consultada por ORM apos o tenant ativo do usuario. A migration `sw20260613a1` habilita/forca RLS na tabela com o guard transacional padrao. (Item **1.3**, parcial.)
- RLS **Leva 84 descontos por canal** — `canal_descontos` e uma tabela viva de campanhas/app/e-commerce com SQL bruto nas rotas; antes da policy, as queries passaram a usar `execute_tenant_safe` com `{tenant_filter}` e sincronizacao RLS no insert. A migration `sx20260613a1` habilita/forca RLS na tabela e adiciona o nome aos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 85 derivados do catalogo de produtos** — `produto_imagens`, `produto_kit_componentes` e `produto_granel_vinculos` usam `BaseTenantModel`, mas o importador de catalogo-base cruza tenant fonte/destino. Antes da policy, o importador passou a sincronizar `app.tenant_id` por operacao: fonte nas leituras e destino nos mappings, buscas, inserts e updates. A migration `sy20260614a1` habilita/forca RLS nas tres tabelas e adiciona o bloco aos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 86 rateio fiscal por canal** — `nota_fiscal_rateio_canal` e `nota_fiscal_item_rateio_canal` usam `BaseTenantModel` e guardam rateios por tenant; a migration `sz20260614a1` habilita/forca RLS nas duas tabelas e registra o bloco nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 87 Chat IA financeiro** — `conversas_ia`, `mensagens_chat` e `contexto_financeiro_chat` usam `BaseTenantModel`; rotas de chat, IA veterinaria e servico da ABA 6 operam com `get_current_user_and_tenant`/`tenant_id` explicito antes de ler ou gravar conversa e mensagens. A migration `ta20260614a1` habilita/forca RLS nas tres tabelas e registra o bloco nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 88 benchmarks de mercado DRE** — `indices_mercado` usa `BaseTenantModel` e é consumida pelas rotas da DRE inteligente por meio da dependency `_usuario_dre`, que valida usuario/tenant e estabelece o contexto transacional antes das leituras ORM. A migration `tb20260614a1` habilita/forca RLS na tabela e registra o nome nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 89 análises DRE legadas** — `dre_produtos`, `dre_categorias_analise`, `dre_comparacoes` e `dre_insights` usam `BaseTenantModel` e são acessadas pelo serviço/rotas da DRE inteligente sob a dependency `_usuario_dre`, com contexto de loja ativo e guard de escrita ORM. A migration `tc20260614a1` habilita/forca RLS nas quatro tabelas e registra o bloco nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 90 períodos DRE** — `dre_periodos` ja estava com `tenant_id` UUID NOT NULL, mixin `TenantScoped`, rotas/helpers sob contexto de loja e testes de isolamento especificos desde a migration `pn20260610a1`. A migration `td20260614a1` habilita/forca RLS na tabela e registra o nome nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 91 baixas de venda** — `venda_baixas` usa `BaseTenantModel` e tem superfície pequena no PDV/entregador: é carregada por relacionamento após `Venda` já filtrada por loja ativa. A migration `te20260614a1` habilita/forca RLS na tabela e registra o nome nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 92 pagamentos de venda** — `venda_pagamentos` usa `BaseTenantModel`; rotas PDV, app e webhooks estabelecem tenant ativo antes de criar/consultar pagamentos, e os SQLs crus de comissão/total já usam `execute_tenant_safe`. A migration `tf20260614a1` habilita/forca RLS na tabela e registra o nome real nos guardrails de SQL bruto tenant-safe/auditoria, preservando o alias legado `vendas_pagamentos` na auditoria. (Item **1.3**, parcial.)
- RLS **Leva 93 itens de venda** — `venda_itens` usa `BaseTenantModel`; rotas, serviços e relatórios operam com tenant ativo ou filtros explícitos, e os SQLs crus de comissões já filtram por tenant. A migration `tg20260614a1` habilita/forca RLS na tabela e registra o nome real nos guardrails de SQL bruto tenant-safe/auditoria, preservando o alias legado `vendas_itens` na auditoria. (Item **1.3**, parcial.)
- RLS **Leva 94 vendas core** — `vendas` usa `BaseTenantModel`; rotas e serviços principais já operam sob tenant ativo ou filtro explícito, e a previsão de demanda de rações saiu de SQL cru direto para `execute_tenant_safe` antes da policy. A migration `th20260614a1` habilita/forca RLS na tabela e mantém `vendas` nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Leva 95 movimentações de estoque** — `estoque_movimentacoes` usa `BaseTenantModel`, já estava nos guardrails de SQL bruto tenant-safe/auditoria e não apareceu com SQL cru runtime direto na auditoria desta fatia. A migration `ti20260614a1` habilita/forca RLS na tabela. `pets` foi adiada para análise própria porque há leitura de pets de tenants parceiros via `vet_partner_link`. (Item **1.3**, parcial.)
- RLS **Leva 96 preparação dos read models** — `read_vendas_resumo_diario`, `read_performance_parceiro` e `read_receita_mensal` continuam sem RLS nesta fatia; antes da policy, os handlers passaram a exigir `tenant_id` ativo e gravar UPSERTs por tenant, os modelos trocaram chaves únicas globais por índices únicos `tenant_id + chave de agregação`, e a migration `tj20260614a1` prepara schemas legados adicionando colunas do `BaseTenantModel` quando necessário e removendo índices únicos globais. Próximo passo antes de RLS: revisar replay/background/health para garantir contexto de tenant em chamadas fora de request. (Item **1.3**, preparação.)
- RLS **Leva 97 preparação do health de read models** — `verificar_saude_read_models` deixou de consultar linhas ORM tenant-scoped sem contexto e passou a checar conexão + existência das tabelas por introspecção de schema, mantendo `/analytics/health` utilizável sem autenticação/tenant antes de uma futura policy RLS. (Item **1.3**, preparação.)
- RLS **Leva 98 preparação do replay de read models** — o replay de eventos de venda passou a resolver `tenant_id` do evento (`tenant_id`, `payload.metadados.tenant_id` ou `metadata.tenant_id`) e executar cada handler dentro de `tenant_context`, abortando com erro claro se um evento replayável não tiver tenant. Eventos novos gravados pelo `EventStore.append()` agora também carregam o tenant ativo em `metadata.tenant_id`, preservando informação suficiente para reprocessamento futuro antes de habilitar RLS nos `read_*`. (Item **1.3**, preparação.)
- RLS **Leva 99 read models** — com handlers, health e replay já tenant-aware, a migration `tk20260614a1` habilita/força RLS em `read_vendas_resumo_diario`, `read_performance_parceiro` e `read_receita_mensal` usando o guard transacional padrão por `app.tenant_id`. (Item **1.3**, parcial.)
- RLS **Leva 100 preparacao de `email_envios`** - `email_envios` continua sem RLS nesta fatia; antes da policy, `EmailQueueService.processar_fila` e `reenviar_email` passaram a exigir/usar tenant explicito, sincronizar `app.tenant_id` e filtrar por `EmailEnvio.tenant_id`. O scheduler global agora processa fila tenant por tenant, e o job diario de acertos tambem descobre tenants ativos antes de consultar parceiros e enfileirar emails. Proximo passo: analisar `emails_templates`/unicidade de `codigo` e, se estiver seguro, criar migration RLS para `email_envios` em leva separada. (Item **1.3**, preparacao.)
- RLS **Leva 101 `email_envios`** - depois da preparacao tenant-aware da fila e do scheduler, a migration `tl20260614a1` habilita/forca RLS em `email_envios` com o guard transacional padrao por `app.tenant_id`. `emails_templates` fica fora desta leva porque o schema legado ainda tem `codigo` unico global e precisa de preparacao propria antes de policy. (Item **1.3**, parcial.)
- RLS **Leva 102 preparacao de `emails_templates`** - `emails_templates` continua sem RLS nesta fatia; antes da policy, o contrato do modelo e a migration `tm20260614a1` trocam a unicidade global de `codigo` por unicidade `tenant_id + codigo`, preservando um indice simples de lookup por `codigo`. Isso permite que lojas diferentes tenham templates com o mesmo codigo sem colisao global, mantendo bloqueio de duplicidade dentro da mesma loja. Proximo passo: revisar renderizacao/uso em jobs e entao habilitar RLS em `emails_templates` em leva separada. (Item **1.3**, preparacao.)
- RLS **Leva 103 `emails_templates`** - depois da preparacao de unicidade por tenant, a migration `tn20260614a1` habilita/forca RLS em `emails_templates` com o guard transacional padrao por `app.tenant_id`. As rotas de templates ja filtram por `tenant_id`, e o scheduler de acertos renderiza templates dentro de `tenant_context` desde a preparacao de `email_envios`. (Item **1.3**, parcial.)
- RLS **Leva 104 preparacao de importacao de extratos** - `arquivos_extrato_importados`, `lancamentos_importados` e `padroes_categorizacao_ia` continuam sem RLS nesta fatia; antes da policy, as rotas da ABA 7, o servico de importacao e o motor de categorizacao passaram a exigir/usar `tenant_id` explicito, corrigindo gravacoes em `usuario_id`, buscas de duplicidade, linkagem financeira, validacao/aprendizado e estatisticas por loja. Proximo passo: habilitar RLS nessas tabelas em migration propria. (Item **1.3**, preparacao.)
- RLS **Leva 105 importacao de extratos** - depois da preparacao tenant-aware da ABA 7, a migration `to20260614a1` habilita/forca RLS em `arquivos_extrato_importados`, `lancamentos_importados` e `padroes_categorizacao_ia` com o guard transacional padrao por `app.tenant_id`. (Item **1.3**, parcial.)
- RLS **Leva 106 preparacao de `configuracao_tributaria`** - `configuracao_tributaria` continua sem RLS nesta fatia; antes da policy, o servico/rotas de tributacao passam a buscar por `tenant_id`, gravar `usuario_id` apenas como auditoria e a migration `tp20260614a1` troca a unicidade global de `usuario_id` por unicidade de `tenant_id`. Os calculos internos de DRE agora resolvem o tenant ativo antes de consultar impostos. Proximo passo: habilitar RLS em `configuracao_tributaria`. (Item **1.3**, preparacao.)
- RLS **Leva 107 `configuracao_tributaria`** - depois da preparacao de chave por tenant, a migration `tq20260614a1` habilita/forca RLS em `configuracao_tributaria` com o guard transacional padrao por `app.tenant_id`. A tabela e acessada por ORM nas rotas/servicos de tributacao ja tenant-aware, sem SQL cru runtime direto nesta fatia. (Item **1.3**, parcial.)
- RLS **Leva 108 acertos de parceiros** - `AcertoService.gerar_acerto` passa a receber `tenant_id`, sincronizar RLS e filtrar parceiro, acerto idempotente e comissoes pendentes pela loja ativa, mantendo `user_id` apenas como auditoria. A migration `tr20260614a1` habilita/forca RLS em `acertos_parceiro` com o guard transacional padrao. (Item **1.3**, parcial.)
- RLS **Leva 109 DRE detalhado por canal** - `DREDetalhadaService` passa a resolver o tenant ativo antes dos calculos por canal, sincronizar RLS, filtrar leituras auxiliares por `tenant_id` e gravar `dre_detalhe_canais`/alocacoes com loja explicita. A migration `ts20260614a1` habilita/forca RLS em `dre_detalhe_canais` e registra a tabela nos guardrails de SQL bruto tenant-safe/auditoria. (Item **1.3**, parcial.)
- RLS **Mapa pós-`rp20260612a1` das próximas tabelas restantes** — depois de `vet_partner_link`, compras/fiscal/Bling, pagamento Mercado Pago, monitor Bling flow, IA fluxo de caixa, Chat IA financeiro, benchmarks DRE, análises DRE legadas, períodos DRE, baixas, pagamentos, itens de venda, vendas core e movimentações de estoque, as próximas fatias devem ser agrupadas por risco: núcleo comercial/PDV (`clientes`, `pets`, `produtos` e derivados de produto restantes); financeiro/DRE/IA (`dre_*` restantes e `lancamentos_importados`); auditoria/operação/read models (`audit_logs`, `email_*`, `read_*`); e acesso multi-tenant (`users`, `user_tenants`), que exige análise própria porque participa do login/seleção de loja antes do contexto ativo. (Item **1.3**, parcial.)
- Testes **0.3** — corrigido o rollback legado de `test_tenant_safe_sql.py::TestBehavior::test_transaction_rollback_preservation`: o teste agora usa `SAVEPOINT` explícito, compatível com a fixture SQLite que faz `commit()` no setup. A bateria RLS voltou a rodar sem `-k not transaction_rollback_preservation`. (Item **0.3**, parcial.)

---

> Este arquivo é vivo: a cada PR, marcamos o item, registramos no log e atualizamos o placar.
