# =============================================================================
# DESENVOLVIMENTO - Ambiente Local (Sem Docker)
# =============================================================================
# Use este arquivo para desenvolvimento diário
# Backend roda direto no Windows, banco SQLite ou PostgreSQL local
# =============================================================================

# -------------------------------
# Identificação do Ambiente
# -------------------------------
ENVIRONMENT=development
DEBUG=true
RUNNING_IN_DOCKER=false

# -------------------------------
# Banco de Dados - DESENVOLVIMENTO
# -------------------------------
# PostgreSQL Local (instalado no Windows)
# Se não tiver PostgreSQL instalado, instale: https://www.postgresql.org/download/windows/
# Ou use Docker: docker run --name petshop-dev-db -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres:16-alpine
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/petshop_dev

# -------------------------------
# Segurança - DESENVOLVIMENTO
# -------------------------------
# Chaves simples para desenvolvimento (não importa ser fraco aqui)
JWT_SECRET_KEY=dev-secret-key-not-for-production
ADMIN_TOKEN=dev_admin_token

# -------------------------------
# CORS - Frontend Local
# -------------------------------
FRONTEND_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:5174,http://127.0.0.1:8000

# -------------------------------
# APIs Externas (OPCIONAL)
# -------------------------------
# Deixe vazio em desenvolvimento se não precisar
GOOGLE_MAPS_API_KEY=
OPENAI_API_KEY=
BLING_CLIENT_ID=
BLING_CLIENT_SECRET=

# Stone Pagamentos (Sandbox/Teste)
STONE_CLIENT_ID=
STONE_CLIENT_SECRET=
STONE_MERCHANT_ID=
STONE_SANDBOX=true

# -------------------------------
# Rate Limiting
# -------------------------------
RATE_LIMIT_ENABLED=false
RATE_LIMIT_PER_MINUTE=1000

# -------------------------------
# Logging
# -------------------------------
LOG_LEVEL=DEBUG

# =============================================================================
# COMO USAR:
# =============================================================================
# 1. Instale PostgreSQL localmente OU rode via Docker:
#    docker run --name petshop-dev-db -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres:16-alpine
# 2. Execute: INICIAR_DEV.bat
# 3. Backend estará em: http://localhost:8000
# 4. Frontend estará em: http://localhost:5173
# =============================================================================
